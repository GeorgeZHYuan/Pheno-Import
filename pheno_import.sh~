#!/bin/bash

source vars

i=0
while IFS='' read -r line || [[ -n "$line" ]]; do
    patients[$i]=$line
	i+=1
done < "$1"

./createTable $patients

$PH_IMPORT_HOME/import/Clinical-data/load_clincial.sh

