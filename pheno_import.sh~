#!/bin/bash

source vars

i=0
while IFS='' read -r line || [[ -n "$line" ]]; do
    patients[$i]=$line
	i+=1
done < "$1"

for i in "${patients[@]}"
do
	echo ${patients[$1]}
done


#./createTable.sh $patients
#$PH_IMPORT_HOME/import/Clinical-data/load_clincial.sh

