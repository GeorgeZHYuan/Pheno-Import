source vars

function getPatient {
	url="$HOST:$PORT/rest/patients/$1"
	response=$(curl -u USER:PSWD -X GET $url)
	echo response >> $LOGFILE
	echo $1
}

function getFamily {
	url="$HOST:$PORT/rest/families"
	request=$(curl -u USER:PSWD -X GET $url -o $LOGFILE)
}
